<UserControl x:Class="GifCapture.Controls.PuncturedRegion"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Visibility="Hidden">
  <Grid>
    <Grid.Resources>
      <SolidColorBrush Color="#66000000" x:Key="SurroundColor" />
      <Style TargetType="Border">
        <Setter Property="Background" Value="{StaticResource SurroundColor}" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Top" />
      </Style>
    </Grid.Resources>

    <Border x:Name="BorderTop" />
    <Border x:Name="BorderBottom" />
    <Border x:Name="BorderLeft" />
    <Border x:Name="BorderRight" />
  </Grid>
</UserControl>